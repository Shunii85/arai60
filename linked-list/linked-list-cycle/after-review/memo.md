レビューを頂いたので、再度考え直したいと思う。

## step1

そもそもコードがちゃんとかけていなかったので、書き直す。

step3のコードを提出してみると、まずcurrent->nextは、currentがnullptrの場合にはアクセスできない。
レビューをもらったとおり、current !=を使うのが自然でもある。

wrong answerになっている。

current = current->nextが抜けている。つまり、次のノードに進んでいない。

## step2

setよりも、unordered_setを使ったほうが良さそう。
なぜかというと、並び順を気にする必要がない。operatorで比較して順序を決定していく。

そして、unordered_setのcontains(要素が含まれているかどうか)メソッドは、最悪でもその時間計算量はO(N)。
平均だとO(1)
c++20でないと使えないらしい。現在よく使われているバージョンは何なのだろう、、、

### 時間計算量について

具体的な計算ステップ数を考えていたので、これは計算量とは異なる。

LinkedListのノード数をN個としたときに、

- N回の探索
  - containsメソッド N回
  - insertメソッド N回

よって、O(N^2)

これに対して、最大の10^4のノード数で考えると、
10^8

※ 最悪計算量を考えている

## step3
